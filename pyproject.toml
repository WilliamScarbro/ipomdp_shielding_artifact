[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ipomdp-shielding"
version = "0.1.0"
description = "IPOMDP shielding experiments"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["ipomdp_shielding*"]
